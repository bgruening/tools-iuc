<tool id="falco" name="Falco" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>An alternative, more performant implementation of FastQC for high throughput sequence quality control</description>
    <macros>
        <token name="@TOOL_VERSION@">2.0.1</token>
        <token name="@VERSION_SUFFIX@">0</token>
        <token name="@PROFILE@">25.0</token>
    </macros>
    <xrefs>
        <xref type="bio.tools">falco</xref>
    </xrefs>
    <requirements>
        <requirement type="package" version="@TOOL_VERSION@">falco</requirement>
    </requirements>
    <version_command>falco --version</version_command>
    <command detect_errors="aggressive"><![CDATA[
        #import re
        #set input_name_sl = re.sub('[^\w\-\s]', '_', str($input_file.element_identifier))

        ln -s '${input_file}' '${input_name_sl}' &&
        falco
            -o outdir
            --threads \${GALAXY_SLOTS:-2}
            #if $contaminants:
                --contaminants '${contaminants}'
            #end if

            #if $adapters:
                --adapters '${adapters}'
            #end if

            #if $config:
                --config '${config}'
            #end if
            $groups
            $tiles
            $bisulfite
            '${input_name_sl}'
        &&
        cp 'outdir/${input_name_sl}/fastqc_report.html' '$html_file' &&
        cp 'outdir/${input_name_sl}/fastqc_data.txt' '$text_file' &&
        cp 'outdir/${input_name_sl}/summary.txt' '$summary_file'
    ]]></command>
    <inputs>
        <param format="fastq,fastq.gz" name="input_file" type="data" label="Raw read data from your current history"/>
        <param argument="--contaminants" type="data" format="tabular" optional="true" label="Contaminant list" help="Tab-delimited file with two columns: name and sequence. Replaces falco's default contaminant list."/>
        <param argument="--adapters" type="data" format="tabular" optional="true" label="Adapter list" help="Tab-delimited file with two columns: name and sequence. Replaces falco's default adapter list."/>
        <param argument="--config" type="data" format="txt" optional="true" label="Configuration file" help="A FastQC-style configuration file that specifies which modules to run and the warning/error thresholds for each module. Lets you disable modules (for example per-tile or duplication) and customise limits."/>
        <param argument="--groups" type="boolean" truevalue="--groups" falsevalue="" checked="false" label="Group base positions" help="Group base positions in plots. Falco 2.0+ reports individual base positions by default; enable this to group positions (equivalent to FastQC's default grouping for reads longer than 50 bp)."/>
        <param argument="--tiles" type="boolean" truevalue="--tiles" falsevalue="--no-tiles" checked="false" label="Per-tile quality analysis" help="Per-tile sequence quality analysis. Only meaningful for Illumina data with tile information in read headers. Disabled by default because falco 2.0.x can fail on some non-standard read headers."/>
        <param argument="--bisulfite" type="boolean" truevalue="--bisulfite" falsevalue="" checked="false" label="Bisulfite sequencing" help="Indicate that reads come from whole-genome bisulfite sequencing so falco accounts for the expected increase in T and decrease in C in the base content."/>
    </inputs>
    <outputs>
        <data format="html" name="html_file" label="${tool.name} on ${on_string}: Webpage"/>
        <data format="txt" name="text_file" label="${tool.name} on ${on_string}: RawData"/>
        <data format="txt" name="summary_file" label="${tool.name} on ${on_string}: SummaryData"/>
    </outputs>
    <tests>
        <!-- Default run on a plain FASTQ file -->
        <test expect_num_outputs="3">
            <param name="input_file" value="1000trimmed.fastq"/>
            <output name="html_file" ftype="html">
                <assert_contents>
                    <has_text text="1000trimmed_fastq - report"/>
                    <has_text text="Falco 2.0.1"/>
                    <has_text text="Basic Statistics"/>
                    <has_text text="Per base sequence quality"/>
                    <has_text text="Per sequence GC content"/>
                    <has_text text="Adapter Content"/>
                </assert_contents>
            </output>
            <output name="text_file" file="fastqc_data.txt" ftype="txt"/>
            <output name="summary_file" file="fastqc_data_summary.txt" ftype="txt"/>
        </test>
        <!-- Run on a gzipped FASTQ file -->
        <test expect_num_outputs="3">
            <param name="input_file" value="1000trimmed.fastq.gz"/>
            <output name="html_file" ftype="html">
                <assert_contents>
                    <has_text text="1000trimmed_fastq_gz - report"/>
                    <has_text text="Falco 2.0.1"/>
                    <has_text text="Per base sequence quality"/>
                    <has_text text="Sequence Duplication Levels"/>
                </assert_contents>
            </output>
            <output name="text_file" file="fastqc_data_gz.txt" ftype="txt"/>
            <output name="summary_file" file="fastqc_data_gz_summary.txt" ftype="txt"/>
        </test>
        <!-- Custom contaminant list -->
        <test expect_num_outputs="3">
            <param name="input_file" value="1000trimmed.fastq"/>
            <param name="contaminants" value="contaminant_list.txt" ftype="tabular"/>
            <output name="html_file" ftype="html">
                <assert_contents>
                    <has_text text="1000trimmed_fastq - report"/>
                    <has_text text="Falco 2.0.1"/>
                </assert_contents>
            </output>
            <output name="text_file" file="fastqc_data_contaminants.txt" ftype="txt"/>
            <output name="summary_file" file="fastqc_data_contaminants_summary.txt" ftype="txt"/>
        </test>
        <!-- Custom adapter list -->
        <test expect_num_outputs="3">
            <param name="input_file" value="1000trimmed.fastq"/>
            <param name="adapters" value="adapter_list.txt" ftype="tabular"/>
            <output name="html_file" ftype="html">
                <assert_contents>
                    <has_text text="1000trimmed_fastq - report"/>
                    <has_text text="Falco 2.0.1"/>
                </assert_contents>
            </output>
            <output name="text_file" file="fastqc_data_adapters.txt" ftype="txt"/>
            <output name="summary_file" file="fastqc_data_adapters_summary.txt" ftype="txt"/>
        </test>
        <!-- Configuration file controlling modules and limits -->
        <test expect_num_outputs="3">
            <param name="input_file" value="1000trimmed.fastq"/>
            <param name="config" value="limits.txt" ftype="txt"/>
            <output name="html_file" ftype="html">
                <assert_contents>
                    <has_text text="1000trimmed_fastq - report"/>
                    <has_text text="Falco 2.0.1"/>
                </assert_contents>
            </output>
            <output name="text_file" file="fastqc_data_customlimits.txt" ftype="txt"/>
            <output name="summary_file" file="fastqc_data_customlimits_summary.txt" ftype="txt"/>
        </test>
        <!-- Grouped base positions -->
        <test expect_num_outputs="3">
            <param name="input_file" value="1000trimmed.fastq"/>
            <param name="groups" value="true"/>
            <output name="html_file" ftype="html">
                <assert_contents>
                    <has_text text="1000trimmed_fastq - report"/>
                    <has_text text="Falco 2.0.1"/>
                </assert_contents>
            </output>
            <output name="text_file" file="fastqc_data_groups.txt" ftype="txt"/>
            <output name="summary_file" file="fastqc_data_groups_summary.txt" ftype="txt"/>
        </test>
        <!-- Bisulfite mode -->
        <test expect_num_outputs="3">
            <param name="input_file" value="1000trimmed.fastq"/>
            <param name="bisulfite" value="true"/>
            <output name="html_file" ftype="html">
                <assert_contents>
                    <has_text text="1000trimmed_fastq - report"/>
                    <has_text text="Falco 2.0.1"/>
                </assert_contents>
            </output>
            <output name="text_file" file="fastqc_data_bisulfite.txt" ftype="txt"/>
            <output name="summary_file" file="fastqc_data_bisulfite_summary.txt" ftype="txt"/>
        </test>
        <!-- Per-tile analysis enabled on Illumina data with valid tile headers.
             Per-tile values are thread-non-deterministic in falco 2.0.x, so the
             outputs are checked with content assertions rather than file diffs. -->
        <test expect_num_outputs="3">
            <param name="input_file" value="illumina_sample.fastq"/>
            <param name="tiles" value="true"/>
            <output name="html_file" ftype="html">
                <assert_contents>
                    <has_text text="illumina_sample_fastq - report"/>
                    <has_text text="Falco 2.0.1"/>
                    <has_text text="Per tile sequence quality"/>
                </assert_contents>
            </output>
            <output name="text_file" ftype="txt">
                <assert_contents>
                    <has_text text="##Falco 2.0.1"/>
                    <has_text text="Filename"/>
                    <has_text text="illumina_sample_fastq"/>
                    <has_text text="&gt;&gt;Per tile sequence quality"/>
                    <has_text text="&gt;&gt;Basic Statistics"/>
                </assert_contents>
            </output>
            <output name="summary_file" ftype="txt">
                <assert_contents>
                    <has_text text="Per tile sequence quality"/>
                    <has_text text="Basic Statistics"/>
                    <has_text text="illumina_sample_fastq"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help format="markdown"><![CDATA[
**What it does**

Falco is a high-speed emulation of the popular FastQC software for quality control of sequencing data. With its superior performance Falco saves computational resources and gives you results faster than FastQC.

The main functions of Falco are very similar to those of FastQC:

- Import of data from FASTQ files (plain or gzip-compressed)
- Providing a quick overview to tell you in which areas there may be problems
- Summary graphs and tables to quickly assess your data
- Export of results to an HTML-based report

The plain text report generated by Falco can be used as a "FastQC" report in MultiQC. Its data is very similar, though not 100% identical, to that generated by FastQC on the same inputs.

-----

**Inputs and outputs**

The wrapper accepts a Galaxy FASTQ or FASTQ.gz dataset as the input read file to check.

Optionally you can provide:

- a **Contaminant list** - a tab-delimited file with two columns (name and sequence) that replaces falco's default contaminant list
- an **Adapter list** - a tab-delimited file with two columns (name and sequence) that replaces falco's default adapter list
- a **Configuration file** - a FastQC-style file that specifies which modules to run and the warning/error thresholds for each module

The tool produces three outputs:

- an **HTML report** with interactive plots
- a **raw data** text file (usable as input to MultiQC)
- a **summary** text file listing the pass/warn/fail status of each module

-----

**Notes on BAM/SAM input**

Falco 2.0.0 was a complete rewrite. BAM and SAM input are currently unreliable in the 2.0.x releases (see upstream issue [#45](https://github.com/smithlabcode/falco/issues/45)), so this wrapper only accepts FASTQ/FASTQ.gz input for now. BAM/SAM support will be re-enabled once the upstream issue is resolved.

-----

**Per-tile quality analysis**

Per-tile sequence quality analysis is only meaningful for Illumina data that carries tile information in the read headers. It is disabled by default because falco 2.0.x can fail on some non-standard headers; enable it only when your reads have valid Illumina tile coordinates.
    ]]></help>
    <citations>
        <citation type="doi">10.12688/f1000research.21142.2</citation>
    </citations>
</tool>
